import { Button } from "@czi-sds/components";

export default function SignIn() {
  return (
    <form>
      <Button type="submit" sdsType="primary" sdsStyle="rounded">
        Sign in with Okta (Unimplemented)
      </Button>
    </form>
  );
}
